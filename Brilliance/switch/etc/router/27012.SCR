ROUTEDESC=登录手机银行后台操作员用户名
[ROUTE]
10 IF GT(LEN(FML(PTERRFLAG)),0) THEN GOTO 30
30 CALL 144 WITH REVMODE 1

47 IF SEQ(FML(retcode),00)   THEN GOTO 70
60 SET retcode=STRFILL(FML(retcode), 600000,8, L )
65 GOTO 100
70 SET retcode=00000000

100 SEND 25 WITH END
