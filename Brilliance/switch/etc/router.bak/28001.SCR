ROUTEDESC=网银向逻辑集中发起交易
[ROUTE]
10 IF NOT(SEQ(FML(PBMB), 101)) THEN GOTO 25
20 SET PIFLAG = 1 
22 GOTO 26
25 SET PIFLAG = 0
26 SET retcode = 00000000


#30 IF SEQ(FML(PICHANNEL), 12) THEN GOTO 100
#40 IF SEQ(FML(PICHANNEL), 13) THEN GOTO 120
#50 IF SEQ(MSGHEAD(PICHANNEL), 15) THEN GOTO 110

100 SEND MSGHEAD(iBegin_q_id) WITH END
#110 SEND 270 WITH END
#120 SEND 231 WITH END
